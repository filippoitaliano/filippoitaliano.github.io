// The version in the footer: calver, `anno.mese.build`. The build is the count
// of changes shipped within that month and starts over at 1 when the month
// turns. Bump it by hand on every change — see the rule in CLAUDE.md.
const SITE_VERSION = '2026.8.1';
