// The version in the footer: calver, `anno.mese.build`. The first two numbers
// say when the version went up; the build is a running count that only ever
// goes forward — it started at 91, the commits on master the day the footer
// was planted, and it never resets. Bump it by hand on every change — see the
// rule in CLAUDE.md.
const SITE_VERSION = '2026.8.91';
